// Seeded PRNG so RandomAgent (and any future stochastic outcome model) is reproducible.
// JS Math.random is not seedable; mulberry32 is a small, fast, well-distributed 32-bit PRNG.
export function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return function () {
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
